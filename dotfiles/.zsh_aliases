[ -f ~/.kubectl_aliases.zsh ] && source ~/.kubectl_aliases.zsh

alias reload="source ~/.zshrc"
alias gbd!="git branch -D"

muse() {
  local tool="$1"
  mise use --global "$tool@$(mise latest "$tool")"
}
