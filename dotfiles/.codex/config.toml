"approval_policy" = "never"
"approvals_reviewer" = "auto_review"
"developer_instructions" = "Honor my requested progress-update cadence. During passive waits, exempt the task from the 60-second commentary cadence and 60-second wait guidance. Use interruptible waits within the tools' supported limits. Do not wake or send updates solely to report unchanged status. Report meaningful progress, failures, blockers, and completion, and respond promptly to user input."
"model" = "gpt-6-astra"
"model_reasoning_effort" = "medium"
"model_reasoning_summary" = "auto"
"sandbox_mode" = "danger-full-access"
"service_tier" = "fast"

["desktop"]
"dock-icon-preference" = "codex-system"
"followUpQueueMode" = "queue"

["features"]
"goals" = true
"memories" = true

["memories"]
"generate_memories" = true
"use_memories" = true

["plugins"]

["plugins"."codex-app-tools@openai-bundled"]
"enabled" = true

["plugins"."documents@openai-primary-runtime"]
"enabled" = true

["plugins"."pdf@openai-primary-runtime"]
"enabled" = true

["plugins"."presentations@openai-primary-runtime"]
"enabled" = true

["plugins"."spreadsheets@openai-primary-runtime"]
"enabled" = true

["plugins"."template-creator@openai-primary-runtime"]
"enabled" = true

["plugins"."visualize@openai-bundled"]
"enabled" = true

["tui"]
"status_line" = ["model-with-reasoning", "current-dir", "git-branch", "total-input-tokens", "total-output-tokens", "context-used", "thread-credits", "estimated-thread-cost", "thread-title", "task-progress", "run-state", "fast-mode"]
"status_line_use_colors" = true
